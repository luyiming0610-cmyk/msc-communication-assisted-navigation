# communication_baseline_20260716

**Purpose**: appears to be an early `state_monitor` node smoke-test
location, predating the `cooperative_avoidance_20260716` and
`fusion_static_neighbor_20260716` experiment work.

**Contents (verified this indexing pass)**: exactly one file,
`state_monitor_20260716_105522.jsonl`, which is **empty (0 bytes)**. No
`bags/`, `config/`, or `logs/` subdirectory exists.

**Status**: not a real experiment — no data was ever captured here. Category
09_legacy_and_excluded (empty/stub, retained not deleted).

**Included in dissertation**: NO.

**Note**: this directory's naming ("communication_baseline") predates and
is unrelated to the current Objective 5 comm-baseline work in
`controller_v4_full_sensor_bypass_20260717/config/comm_baseline_v1/` —
do not confuse the two.
