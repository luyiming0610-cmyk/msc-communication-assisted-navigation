# objective5_impairment_matrix_v1_condition_E_trial04_attempt01

Formal Condition E Trial 04: independent per-message loss (`drop_probability=0.15`), no configured delay, jitter, or outage.

Start with `final_verdict.json` and `summary.md`. Raw bag and diagnostic logs are preserved outside Git in native WSL and in the gitignored Windows `bags/` copy; hashes were verified file by file.
